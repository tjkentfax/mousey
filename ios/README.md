# Mousey iPhone app

1. Open `Mousey.xcodeproj` in Xcode on a Mac.
2. Select your iPhone as the run destination.
3. In **Signing & Capabilities**, select your Apple developer team.
4. Build and run.
5. Allow Motion and Local Network permissions when prompted.

The app sends mouse events to the desktop receiver over HTTP on port 8765.
